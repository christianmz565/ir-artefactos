import { Button } from "@/components/Button/Button";

export function IGU_7_TAB_3_BTN_1({ id }: { id: string }) {
  return (
    <Button size="sm" id={`igu-7-tab-3-btn-1-edit-${id}`}>
      Editar
    </Button>
  );
}
