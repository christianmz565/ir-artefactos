import { Button } from "@/components/Button/Button";

export function IGU_7_TAB_3_BTN_2({ id }: { id: string }) {
  return (
    <Button size="sm" variant="danger" id={`igu-7-tab-3-btn-2-del-${id}`}>
      Eliminar
    </Button>
  );
}
