import { Button } from "@/components/Button/Button";

export function IGU_7_TAB_3_BTN_3({ id }: { id: string }) {
  return (
    <Button size="sm" variant="info" id={`igu-7-tab-3-btn-3-view-${id}`}>
      Ver Detalle
    </Button>
  );
}
