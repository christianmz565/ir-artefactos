import { Button } from "@/components/Button/Button";

export function IGU_32_TAB_3_BTN_2({ id }: { id: string }) {
  return (
    <Button size="sm" variant="primary" id={`igu-32-tab-3-btn-2-edit-${id}`}>
      Editar
    </Button>
  );
}
