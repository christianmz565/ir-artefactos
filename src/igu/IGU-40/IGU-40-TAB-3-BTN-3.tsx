import { Button } from "@/components/Button/Button";

export function IGU_40_TAB_3_BTN_3({ id }: { id: string }) {
  return (
    <Button size="sm" variant="danger" id={`igu-40-tab-3-btn-3-inact-${id}`}>
      Inactivar
    </Button>
  );
}
