import { Button } from "@/components/Button/Button";

export function IGU_29_TAB_3_BTN_3({ id }: { id: string }) {
  return (
    <Button size="sm" variant="danger" id={`igu-29-tab-3-btn-3-del-${id}`}>
      Eliminar
    </Button>
  );
}
