import { Button } from "@/components/Button/Button";

export function IGU_29_TAB_3_BTN_1({ id }: { id: string }) {
  return (
    <Button size="sm" id={`igu-29-tab-3-btn-1-view-${id}`}>
      Ver
    </Button>
  );
}
