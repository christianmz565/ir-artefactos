import { Button } from "@/components/Button/Button";

export function IGU_2_TAB_3_BTN_2({ id }: { id: string }) {
  return (
    <Button size="sm" id={`igu-2-tab-3-btn-2-edit-${id}`}>
      Editar
    </Button>
  );
}
