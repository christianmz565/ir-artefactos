import { Input } from "@/components/Input/Input";

export function SearchNotificacion() {
  return (
    <Input
      label="Buscar"
      placeholder="Buscar por nombre del apoderado..."
      id="INPUT-SEARCH-NOTIFICACION"
    />
  );
}
