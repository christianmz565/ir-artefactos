import { Button } from "@/components/Button/Button";

export function IGU_36_TAB_3_BTN_1({ id }: { id: string }) {
  return (
    <Button size="sm" variant="info" id={`igu-36-tab-3-btn-1-view-${id}`}>
      Ver
    </Button>
  );
}
