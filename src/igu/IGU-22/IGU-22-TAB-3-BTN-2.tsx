import { Button } from "@/components/Button/Button";

export function IGU_22_TAB_3_BTN_2({ id }: { id: string }) {
  return (
    <Button size="sm" variant="danger" id={`igu-22-tab-3-btn-2-inact-${id}`}>
      Inactivar
    </Button>
  );
}
