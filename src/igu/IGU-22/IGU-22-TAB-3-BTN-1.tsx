import { Button } from "@/components/Button/Button";

export function IGU_22_TAB_3_BTN_1({ id }: { id: string }) {
  return (
    <Button size="sm" variant="info" id={`igu-22-tab-3-btn-1-view-${id}`}>
      Ver
    </Button>
  );
}
